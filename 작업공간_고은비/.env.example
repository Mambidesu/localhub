# OpenAI API key for client-side usage (WARNING: embedding keys in frontend exposes them to users)
# Use a key with strict usage limits and low billing limits.
VITE_OPENAI_KEY=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

# Note: Vite env variables prefixed with VITE_ are embedded into the built bundle.
# Do NOT put a sensitive fully-funded key here in production. Consider proxying requests via a backend for production use.

# NOTE: External web search via SerpAPI is disabled in this project by default.
# If you intentionally want to re-enable a server-side search proxy, add SERPAPI_KEY to server/.env.
# Example server .env content (server/.env):
# SERPAPI_KEY=your_serpapi_key_here

# Optional: If you run a local server to persist generated posts, set its base URL here so the client can POST new items.
# Example:
# VITE_SERVER_API_URL=http://localhost:4321
